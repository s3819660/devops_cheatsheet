# remove docker image if it exists

# remote docker container if it exists

# remove all docker images

# remove all docker containers

# build from dockerfile: docker build -t <image-name> -f <Dockefile-name> /path/to/dockerfile

